services:
  - type: web
    name: muraldisc-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "cd discord_profile/backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
